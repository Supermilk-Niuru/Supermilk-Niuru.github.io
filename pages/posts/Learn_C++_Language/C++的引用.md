---
title: C++的引用
date: 2026-02-13
categories:
  - Computer Science
tags:
  - C++
---
### C++的引用
###### 现在先简单说一下
C++的引用相当于一个起别名，直接传了地址（但是不同于指针，指针可以改变指向，而引用是不可以改变的，并且引用必须**初始化**），不需要像值传递那样要重新拷贝，申请新的内存，也不会像传递指针那样出现野指针的情况。
引用的好处是，底层逻辑是指针操作，但是在操作的时候和传入值的语法操作是一致的。也可以对传入的值进行修改。
如果要避免被修改的话，可以使用常量引用的写法`const &……`